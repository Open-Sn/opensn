name: clang-tidy

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: [main]
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: [self-hosted]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: build
        shell: bash
        run: |
          module load python3/3.12.3 opensn/clang/22
          mkdir build
          cd build
          cmake \
            -DBLAS_LIBRARIES="/opt/local/opensn/clang/22.1.0/dependencies/lib/libf2cblas.a" \
            -DLAPACK_LIBRARIES="/opt/local/opensn/clang/22.1.0/dependencies/lib/libf2cblas.a" \
            -DOPENSN_WITH_PYTHON_MODULE=ON ..
          make -j
      - name: clang-tidy
        shell: bash
        run: |
          module load python3/3.12.3 opensn/clang/22
          tools/developer/run-clang-tidy.sh -extra-arg-before=--gcc-toolchain=/opt/local/opensn/gcc/14.2.0/gcc-14.2.0 \
                                            -extra-arg=-I/opt/local/opensn/clang/22.1.0/mpich-5.0.1/include
